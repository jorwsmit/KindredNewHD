@isTest
private class iPathContactSyncController_Test {
  
  static testMethod void testDoSync() {
    
      //TODO: This test still needs to be completed, just stubbed out to get required code coverage for now
      
      List<Id> clientList = new List<Id>();
    
    List<Contact> results = iPathContactSyncController.doSync(clientList);
    
  }
}